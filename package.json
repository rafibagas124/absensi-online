{
  "name": "absensipro-build",
  "version": "1.0.0",
  "private": true,
  "description": "Build/obfuscation pipeline untuk AbsensiPro (menghasilkan folder dist/ siap deploy)",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.1"
  },
  "engines": {
    "node": ">=16.7.0"
  }
}
